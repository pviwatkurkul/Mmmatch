import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['images.pexels.com'], // replace with your actual image domain
  },
};

export default nextConfig;
